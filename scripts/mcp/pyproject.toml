[project]
name = "mcp-router"
version = "0.1.0"
description = "Meta-MCP router: on-demand activation of backend MCP servers"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0",
    "anyio>=4.4.0",
]
